import { Modal } from 'react-bootstrap-v5';

const MenuModal = (props) => {
    let {showModal} = props;

    return (
        <div className="board-modal-menu-container">
            Test Modal
        </div>
    )
}

export default MenuModal;